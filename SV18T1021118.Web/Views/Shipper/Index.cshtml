@model SV18T1021118.Web.Models.PaginationSearchInput
@{
    ViewBag.Title = "Người giao hàng";
}

<!-- search form -->
<form id="searchInput" action="~/shipper/search">
    <div class="input-group">
        @Html.HiddenFor(m => m.PageSize)
        @if (@Session["SHIPPER_NAME"] != null)
        {
            <input type="text" name="searchValue" class="form-control" placeholder="Nhập thông tin cần tìm kiếm ... " autofocus value="@Session["SHIPPER_NAME"]">
        }
        else
        {
            <input type="text" name="searchValue" class="form-control" placeholder="Nhập thông tin cần tìm kiếm ... " autofocus value="@Model.SearchValue">
        }
        <span class="input-group-btn">
            <button type="submit" id="search-btn" class="btn btn-flat btn-primary">
                <i class="fa fa-search"></i>
            </button>
            <a href="~/shipper/create" class="btn btn-flat btn-success" style="margin-left: 5px"><i class="fa fa-address-book"></i> Bổ sung </a>
        </span>
    </div>
</form>

<div id="searchResult">

</div>

@section scripts{
    <script src="~/Scripts/Search.js"></script>
    <script>
        $(document).ready(function(){
            doSearch(@Model.Page);
            $("#searchInput").submit(function (e){
                e.preventDefault();
                doSearch(1);
                return false;
            });
        });
    </script>
}





